name: MCP Server
version: "0.1.1"
slug: mcp
description: Python stdio MCP server for n8n
arch:
  - amd64
  - aarch64
startup: services
boot: auto
stdin: true
options:
  webhook_url: ""
schema:
  webhook_url: str
